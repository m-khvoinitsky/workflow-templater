summary: Release server version {{ version }}
project:
  key: PRJ
issuetype:
  name: Sub-task
parent:
  key: '{{ issuekey_1_main }}'
components:
- name: Releases
priority:
  name: Medium
labels:
- 'version_{{ version }}'
assignee:
  name: '{{ deploy_engineer }}'
description: |-
  * update config files
  * run this script
  * run that script
